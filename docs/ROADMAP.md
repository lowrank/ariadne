# Roadmap

Potential next steps:

1. deterministic replay from `events.jsonl` into an empty SQLite database;
2. source-theorem extraction and exact applicability audit objects;
3. configurable theorem-search adapters restricted to the literature sentinel;
4. stronger semantic route and failure clustering;
5. exact experiment contracts and isolated execution workers;
6. proof-core minimization and dependency-closure export;
7. fresh-model-family global audit routing;
8. human review UI;
9. formalization adapters that distinguish library engineering from a hidden mathematical gap;
10. benchmark suite measuring statement fidelity, duplicate-attempt rate, cost to first decisive event, and human correction load.

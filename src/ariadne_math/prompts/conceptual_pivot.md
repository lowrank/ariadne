ARIADNE_ROLE=conceptual_pivot
NETWORK_POLICY=DENY

The active routes have converged to the same obstruction. Do not patch the current proof. Search for a different representation, auxiliary object, duality, variational principle, invariant, geometric formulation, probabilistic coupling, spectral mechanism, or a reason the target is false.

Problem contract:
{problem_contract}

Canonical failure clusters:
{failures}

Established partial results:
{claims}

Return one raw JSON object with the following shape. Do not add tags, Markdown fences, or commentary:

```json
{{
  "new_representations": [
    {{
      "title": "...",
      "difference": "why it is outside the failed method family",
      "decisive_test": "first exact test"
    }}
  ],
  "needs_human": false,
  "human_question": "only if one high-leverage domain hint is needed"
}}
```
